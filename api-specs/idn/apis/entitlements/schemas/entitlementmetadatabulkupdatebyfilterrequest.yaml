description: >-
  Request to bulk update Access Model Metadata on every entitlement matching a filter expression. Adding or replacing
  custom metadata requires a suite license.
type: object
properties:
  filters:
    description: |-
      Filter results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results)

      Filtering is supported for the following fields and operators:

      **id**: *eq*
    type: string
    example: id eq 2c9180867817ac4d017817c491119a20
  operation:
    description: The operation to be performed
    type: string
    enum:
      - ADD
      - REMOVE
      - REPLACE
    example: REPLACE
  replaceScope:
    description: >-
      The choice of update scope. **ATTRIBUTE** replaces only the values of the attributes named in `values`, and
      **ALL** replaces every metadata attribute on the entitlement.
    type: string
    enum:
      - ALL
      - ATTRIBUTE
    example: ATTRIBUTE
  values:
    description: The metadata to be updated, including attribute key and value.
    type: array
    nullable: false
    items:
      type: object
      required:
        - attribute
        - values
      properties:
        attribute:
          type: string
          description: The technical name of the metadata attribute.
          example: iscFederalClassifications
        values:
          type: array
          description: The values of the attribute to be updated.
          items:
            type: string
            example: secret
          nullable: true
          example:
            - secret
        objectType:
          type: string
          description: >-
            The type of the metadata attribute. Set to `custom` for custom metadata attributes, which require a suite
            license.
          example: custom
    example:
      - attribute: iscFederalClassifications
        values:
          - topSecret
required:
  - filters
  - operation
  - values
