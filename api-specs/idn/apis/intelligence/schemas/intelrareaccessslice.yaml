type: object
required:
  - items
description: Rare access slice embedded in the aggregate identity response.
properties:
  items:
    type: array
    description: First page of rare access items for the identity.
    items:
      $ref: ./inteloutlieraccessitem.yaml
  next:
    type: string
    format: uri
    description: Absolute URL to the next rareAccess page; present only when more results exist.
    example: https://tenant.example.api.cloud.sailpoint.com/intelligence/identities/v1/ef38f94347e94562b5bb8424a56397d8/outliers/rare-access?limit=10&offset=10
