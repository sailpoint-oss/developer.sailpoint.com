openapi: 3.0.1
info:
  title: Identity Security Cloud API - JIT Activations
  description: >-
    Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with
    greater scalability. We encourage you to join the SailPoint Developer Community forum at
    https://developer.sailpoint.com/discuss to connect with other developers using our APIs.
  termsOfService: https://developer.sailpoint.com/discuss/tos
  contact:
    name: Developer Relations
    url: https://developer.sailpoint.com/discuss/api-help
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
  version: v1
servers:
  - url: https://{tenant}.api.identitynow.com
    description: This is the production API server.
    variables:
      tenant:
        default: sailpoint
        description: This is the name of your tenant, typically your company's name.
  - url: https://{apiUrl}
    description: This is the versioned API server.
    variables:
      apiUrl:
        default: sailpoint.api.identitynow.com
        description: This is the api url of your tenant
tags:
  - name: JIT Activations
    description: |
      Use this API to start and manage Just-In-Time (JIT) Privileged activation workflows for entitlement connections,
      and to search activation history.

      OAuth scopes: **idn:jit-activation-workflow:*** (activate, extend, deactivate, manage) for workflow APIs.
      **idn:jit-activation-history:read** (admin history view) and **idn:jit-activation-history-self:read** (self history view).
paths:
  /jit-activations/v1/activate:
    $ref: ./paths/jit-activations-v1-activate.yaml
  /jit-activations/v1/deactivate:
    $ref: ./paths/jit-activations-v1-deactivate.yaml
  /jit-activations/v1/extend:
    $ref: ./paths/jit-activations-v1-extend.yaml
  /jit-activation-history/v1:
    $ref: ./paths/jit-activations-v1-history.yaml
  /jit-activation-history/v1/current-identity:
    $ref: ./paths/jit-activations-v1-history-current-identity.yaml
security:
  - userAuth:
      - sp:scopes:all
