openapi: 3.0.1
info:
  title: Identity Security Cloud API - Managed Clusters
  description: >-
    Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with
    greater scalability. We encourage you to join the SailPoint Developer Community forum at
    https://developer.sailpoint.com/discuss to connect with other developers using our APIs.
  termsOfService: https://developer.sailpoint.com/discuss/tos
  contact:
    name: Developer Relations
    url: https://developer.sailpoint.com/discuss/api-help
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
  version: v1
servers:
  - url: https://{tenant}.api.identitynow.com
    description: This is the production API server.
    variables:
      tenant:
        default: sailpoint
        description: This is the name of your tenant, typically your company's name.
  - url: https://{apiUrl}
    description: This is the versioned API server.
    variables:
      apiUrl:
        default: sailpoint.api.identitynow.com
        description: This is the api url of your tenant
tags:
  - name: Managed Clusters
    description: >
      Use this API to implement managed cluster functionality. 

      With this functionality in place, administrators can modify and delete existing managed clients, get their
      statuses, and create new ones. 
paths:
  /managed-clusters/v1:
    $ref: ./paths/managed-clusters-v1.yaml
  /managed-clusters/v1/{id}:
    $ref: ./paths/managed-clusters-v1-by-id.yaml
  /managed-clusters/v1/{id}/log-config:
    $ref: ./paths/managed-clusters-v1-by-id-log-config.yaml
  /managed-clusters/v1/{id}/manualUpgrade:
    $ref: ./paths/managed-clusters-v1-by-id-manualupgrade.yaml
security:
  - userAuth:
      - sp:scopes:all
