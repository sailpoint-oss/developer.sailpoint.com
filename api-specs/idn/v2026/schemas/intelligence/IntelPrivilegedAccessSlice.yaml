type: object
required:
  - items
description: Full privileged access result embedded in the aggregate identity response.
properties:
  items:
    type: array
    description: Privileged access items for the identity.
    items:
      $ref: ./IntelPrivilegedAccessItemWire.yaml
