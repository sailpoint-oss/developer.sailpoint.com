required: true
content:
  application/json:
    schema:
      type: object
      properties:
        user_roles:
          type: array
          items:
            $ref: "../../schemas/PATCH/UserRoles.yaml"