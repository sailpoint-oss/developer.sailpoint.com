required: true
content:
  application/json:
    schema:
      type: object
      properties:
        user_profiles:
          type: array
          items:
            $ref: "../../schemas/POST/UserProfile.yaml"