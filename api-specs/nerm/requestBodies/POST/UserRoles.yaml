required: true
content:
  application/json:
    schema:
      type: object
      properties:
        user_roles:
          type: array
          items:
            $ref: "../../schemas/POST/UserRole.yaml"