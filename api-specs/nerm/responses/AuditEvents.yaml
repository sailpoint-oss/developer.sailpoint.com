description: AuditEvents
content:
  application/json:
    schema:
      type: object
      properties:
        audit_events:
          type: Array
          items:
            $ref: '../schemas/POST/AuditEvent.yaml'
