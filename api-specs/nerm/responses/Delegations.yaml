description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        delegations:
          type: array
          items:
            $ref: '../schemas/GET/Delegation.yaml'
        _metadata:
          $ref: '../schemas/Metadata.yaml'