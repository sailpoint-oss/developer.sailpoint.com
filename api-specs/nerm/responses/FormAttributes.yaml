description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        form_attribute:
          type: object
          $ref: '../schemas/GET/FormAttributes.yaml'
