description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        page_elements:
          type: array
          items:
            $ref: '../schemas/GET/PageElement.yaml'