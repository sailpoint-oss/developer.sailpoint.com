description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        page:
          type: object
          $ref: '../schemas/GET/Pages.yaml'