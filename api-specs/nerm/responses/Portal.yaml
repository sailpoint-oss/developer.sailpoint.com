description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        portal:
          type: object
          $ref: "../schemas/GET/Portal.yaml"
