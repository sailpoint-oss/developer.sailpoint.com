description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        profile_type_namings:
          type: array
          items:
            $ref: '../schemas/GET/ProfileTypeNaming.yaml'
        _metadata:
          $ref: '../schemas/Metadata.yaml'
