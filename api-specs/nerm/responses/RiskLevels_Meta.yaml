description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        risk_levels:
          type: array
          items:
            $ref: '../schemas/GET/RiskLevel.yaml'
        _metadata:
          $ref: '../schemas/Metadata.yaml'