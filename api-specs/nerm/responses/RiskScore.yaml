description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        risk_score:
          type: object
          $ref: '../schemas/GET/RiskScore.yaml'