description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        role_profiles:
          type: array
          items:
            $ref: '../schemas/GET/RoleProfile.yaml'
        _metadata:
          $ref: '../schemas/Metadata.yaml'