description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        user_role:
          type: object
          $ref: '../schemas/GET/UserRole.yaml'