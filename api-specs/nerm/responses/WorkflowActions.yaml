description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        workflow_actions:
          type: object
          $ref: '../schemas/GET/WorkflowAction.yaml'