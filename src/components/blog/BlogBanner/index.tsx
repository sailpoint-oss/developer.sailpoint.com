import React from 'react';
import styles from './styles.module.css';

const BlogBanner: React.FC = () => {
  return <div className={styles.titleContainer}>{/* <h1 className={styles.blogTitle}>Blog</h1> */}</div>;
};

export default BlogBanner;
