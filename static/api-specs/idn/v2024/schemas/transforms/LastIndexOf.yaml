title: lastIndexOf
type: object
required:
- substring
properties:
  substring:
    type: string
    description: A substring to search for, searches the entire string provided, and returns the index of the last occurrence of the specified substring.
    example: 'admin_'
  requiresPeriodicRefresh:
    $ref: './RequiresPeriodicRefresh.yaml'
  input:
    $ref: './input.yaml'