type: object
properties:
  detailCode:
    type: string
    description: Machine-readable error code returned by the Intelligence Package service.
    example: IDC_BAD_REQUEST
  message:
    type: string
    description: Human-readable explanation of the error suitable for client logging.
    example: The filters query parameter is required and cannot be empty.
