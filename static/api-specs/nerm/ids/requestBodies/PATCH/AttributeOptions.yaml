required: true
content:
  application/json:
    schema:
      type: object
      properties:
        ne_attribute_options:
          type: array
          items:
            $ref: "../../schemas/PATCH/AttributeOptions.yaml"