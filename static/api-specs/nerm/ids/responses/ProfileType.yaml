description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        profile_type:
          type: object
          $ref: '../schemas/GET/ProfileType.yaml'