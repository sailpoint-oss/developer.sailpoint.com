description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        roles:
          type: array
          items:
            $ref: '../schemas/GET/Role.yaml'
        _metadata:
          $ref: '../schemas/Metadata.yaml'