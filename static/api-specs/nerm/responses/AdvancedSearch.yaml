description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        advanced_search:
          type: object
          $ref: '../schemas/GET/AdvancedSearch.yaml'