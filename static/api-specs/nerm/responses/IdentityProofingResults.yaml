description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        identity_proofing_results:
          type: array
          items:
            $ref: '../schemas/GET/IdentityProofingResult.yaml'
        _metadata:
          $ref: '../schemas/Metadata.yaml'