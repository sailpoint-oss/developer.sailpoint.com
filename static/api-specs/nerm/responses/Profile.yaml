description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        profile:
          type: object
          $ref: '../schemas/GET/Profile.yaml'