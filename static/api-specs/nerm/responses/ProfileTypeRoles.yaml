description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        form:
          type: object
          $ref: '../schemas/GET/ProfileTypeRoles.yaml'