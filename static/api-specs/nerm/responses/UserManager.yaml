description: Expected response to a valid request
content:
  application/json:
    schema:
      type: object
      properties:
        user_manager:
          type: object
          $ref: '../schemas/GET/UserManager.yaml'