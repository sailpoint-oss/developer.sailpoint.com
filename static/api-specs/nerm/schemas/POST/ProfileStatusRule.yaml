type: object
required:
  - type
  - comparison_operator
  - value
properties:
  type:
    type: string
    enum: [ProfileStatusRule]
  comparison_operator:
    type: string
    enum: ['==', '!=']
  value:
    type: string
    enum: [Active, Inactive, Leave of absence, Terminated]
