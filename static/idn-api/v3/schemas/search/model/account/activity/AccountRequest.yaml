type: object
properties:
  accountId:
    type: string
  attributeRequests:
    type: array
    items:
      $ref: 'AttributeRequest.yaml'
  op:
    type: string
  provisioningTarget:
    $ref: '../Source.yaml'
  result:
    type: object
    properties:
      errors:
        type: array
        items:
          type: string
      status:
        type: string
      ticketId:
        type: string
  source:
    $ref: '../Source.yaml'
