title: static
type: object
required:
- values
properties:
  values:
    type: string
    description: This must evaluate to a JSON string, either through a fixed value or through conditional logic using the Apache Velocity Template Language.
    example: "string$variable"
    externalDocs:
      description: Static Transform Documentation 
      url: https://developer.sailpoint.com/docs/transforms/transform_operations/operations/static.html
  requiresPeriodicRefresh:
    $ref: './RequiresPeriodicRefresh.yaml'